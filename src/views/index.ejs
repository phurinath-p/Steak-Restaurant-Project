<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link rel="icon" href="/images/logo.png" type="image/x-icon">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Kanit', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            color: #333;
            text-align: center;
            overflow: hidden;
 
            position: relative;
        }

        .background {
            position: absolute;
            top: -10%;
  
            left: -10%;
  
            width: 120%;

            height: 120%;

            background-image: url('/images/BG.png');

            background-size: cover;
            background-position: center;
            filter: blur(10px) brightness(0.7);
       
            z-index: -1;
          
        }

        .card-logo {
            max-width: 150px;
   
            height: auto;
       
            transition: transform 0.3s ease;
        }

        .card-logo:hover {
            transform: scale(1.1);
        }

        .card {
            background-color: rgba(255, 255, 255, 0.7);
       
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
            padding: 60px 80px;
            text-align: center;
            transition: transform 0.2s, box-shadow 0.2s;
            z-index: 1;
        
            width: 600px;
        }

        h1 {
            font-size: 2.3rem;
            margin-bottom: 20px;
        }

        p {
            font-size: 1.2rem;
            margin-bottom: 40px;
            color: #555;
        }

        .btn {
            padding: 10px 20px;
            font-size: 1rem;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <div class="background"></div>
    <div class="card">
        <img src="/images/logo.png" alt="Store Logo" class="card-logo">
        <p>สวัสดีคุณ "<%= username %>"</p>
        <h1>ยินดีต้อนรับสู่ Steak Restaurant</h1>
        
        <p>สเต็กหอมกรุ่น เสิร์ฟพร้อมรอยยิ้ม ในบรรยากาศอบอุ่นเหมือนบ้าน</p>
        <a href="/products" class="btn">เมนูสินค้า</a>
    </div>

</body>

</html>
